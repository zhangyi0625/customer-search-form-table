"use strict";
'use client';

var _antd = require("antd");
var _client = require("react-dom/client");
(0, _antd.unstableSetRender)(function (node, container) {
  container._reactRoot || (container._reactRoot = (0, _client.createRoot)(container));
  var root = container._reactRoot;
  root.render(node);
  return function () {
    return new Promise(function (resolve) {
      setTimeout(function () {
        root.unmount();
        resolve();
      }, 0);
    });
  };
});